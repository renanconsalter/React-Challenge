import React from 'react';

import * as styled from './index.style';

const PlansContainer = ({ children }) => (
  <styled.PlansContainer>
    {children}
  </styled.PlansContainer>
);

export default PlansContainer;
